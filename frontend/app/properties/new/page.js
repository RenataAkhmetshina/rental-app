import PropertyForm from '../../../components/property/PropertyForm';

export const metadata = { title: 'List a Property — RentNest' };

export default function NewPropertyPage() {
  return <PropertyForm />;
}
